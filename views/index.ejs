<h1>ffff</h1>

<% blogs.forEach(function(blog){ %>
	<div>
		<h2> <%= blog.title %></h2>
		<span> <%= blog.created %> </span>
		<img src="<%= blog.image %>">
		<p> <%= blog.body %></p>
	</div>
<% }) %>